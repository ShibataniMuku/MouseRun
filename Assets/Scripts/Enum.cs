public enum TravelDirection // 上下左右の方向
{
    up = 0,
    right = 1,
    down = 2,
    left = 3
}

public enum Way // 回転の方向
{
    clockwise,
    counterclockwise
}

public enum SceneEnum // シーン名
{
    Title,
    Main, 
    Result
}